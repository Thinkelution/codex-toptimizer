[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tasklean"
version = "0.2.0"
description = "Local Codex efficiency audits and measured prompt preparation"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
tasklean = "tasklean.cli:main"

[tool.setuptools]
packages = ["tasklean"]
